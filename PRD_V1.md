# 產品需求文件 (PRD) V1：家庭透明化記帳系統 (MVP)

### 0. 文件資訊
| 項目 | 內容 |
|------|------|
| 文件版本 | 1.1 (V1 正式版) |
| 建立日期 | 2026年5月16日 |
| 最後更新 | 2026年5月16日 |
| 文件狀態 | 已落地實作 |
| 文件擁有者 | 產品經理 / Gemini CLI |

---

### 1. 簡介 (已更新)
- **1.1 文件目的**：紀錄「家庭透明化記帳系統」MVP 階段的最終實作需求與技術決策。
- **1.4 MVP 目標實作狀況**：
    * **身分驗證**：已實現 JWT 基礎的註冊、登入流。
    * **AI OCR**：已整合本地 Tesseract OCR，支援金額與日期自動提取。
    * **極簡輸入**：實作了包含 0-9、小數點、倒退鍵與清空鍵的自定義虛擬鍵盤。
    * **公私帳機制**：實作了資料庫層級的權限過濾，確保私帳僅本人可見。
    * **報表中心**：實作了按類別彙整與支出佔比的統計 API。

---

### 4. 產品功能 (V1 落地細節)

#### 4.1 使用者帳戶與家庭群組
- **家庭建立流程**：強制性設計。使用者登入後若無家庭，必須「建立家庭」才能進行後續操作。
- **邀請機制**：實作了基於 UUID 的 8 位元大寫邀請碼。

#### 4.2 AI 拍照辨識 (OCR) 模組
- **實作技術**：採用 `pytesseract` + 正則表達式 (Regex) 解析。
- **辨識邏輯**：從圖片文字中自動搜尋金額（浮點數格式）與日期（YYYY/MM/DD 格式）。
- **圖片處理**：上傳圖片會被賦予 UUID 檔名並儲存於後端靜態目錄 `static/uploads`。

#### 4.3 快速手動輸入模組
- **虛擬鍵盤**：為行動端優化的網頁版鍵盤，避免喚起系統原生鍵盤遮擋 UI。
- **支出屬性**：預設為「公帳」，點擊 Toggle 可切換為「私帳」。

#### 4.4 家庭帳本管理
- **隱私過濾邏輯**：
    * 顯示所屬家庭的所有 `is_public = True` 交易。
    * 僅顯示屬於目前登入使用者的 `is_public = False` 交易。

---

### 9. 假設與約束 (V1 更新)
- **相容性修復**：解決了 `passlib` 與最新版 `bcrypt` 的相容性衝突，鎖定 `bcrypt==4.0.1`。
- **路徑標準化**：統一採用 `127.0.0.1` 取代 `localhost` 以解決 Windows 環境下的導向問題。
- **API 規範**：移除 API 末尾斜線要求，確保前後端請求路徑嚴格匹配。
